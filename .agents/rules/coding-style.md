# 编码风格

- TypeScript/React 使用 camelCase；Python 使用 snake_case；组件使用 PascalCase
- 函数/方法保持简短、单一职责
- 文件长度建议不超过 300 行；超过时优先按职责拆分
- API Router 只做路由、校验和异常转换；业务逻辑放在 service 层
- 注释只解释关键约束或复杂逻辑，不注释显而易见的代码
- 本文件只记录长期规则，临时实现细节不要写入

